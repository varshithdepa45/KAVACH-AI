# KAVACH AI backend dependencies
#
# CORE (required - the app will not start without these):
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6

# OPTIONAL (import-guarded in code; the app STARTS even if these are absent).
# Pure-python-ish and pip-installable offline-friendly:
fpdf2>=2.7          # PDF generation (demo data + deliverables)
Pillow>=10.0        # P&ID PNG generation

# Optional richer deliverables (guarded):
python-docx>=1.1    # .docx reports
openpyxl>=3.1       # .xlsx findings matrix
reportlab>=4.0      # alternative PDF backend

# Optional RAG upgrade (guarded; falls back to pure-python hash embeddings):
# chromadb           # vector store (heavy; skip for a minimal offline install)
# sentence-transformers  # real embeddings (heavy; skip for minimal install)
